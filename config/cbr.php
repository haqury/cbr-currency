<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Проверка SSL при запросах к API ЦБ
    |--------------------------------------------------------------------------
    | В production всегда true. Для локальной разработки без CA bundle можно false.
    */
    'verify_ssl' => (bool) filter_var(env('CBR_VERIFY_SSL', true), FILTER_VALIDATE_BOOLEAN),

    /*
    |--------------------------------------------------------------------------
    | Максимум дней назад при поиске предыдущего торгового дня
    |--------------------------------------------------------------------------
    | Используется в RatesController при fallback-переборе по ЦБ, если в БД нет данных.
    */
    'max_days_back' => (int) env('CBR_MAX_DAYS_BACK', 365),
];
